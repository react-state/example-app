import * as React from "react";

export class RouterHistory extends React.Component<any, undefined> {
    render() {
        return (<div>This is router history test for window.state.showHistory()</div>)
    }
}